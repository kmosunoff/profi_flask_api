Endpoint returning a note by its id.
---
parameters:
  - name: id
    in: path
    type: int
    required: true
responses:
  200:
    description: A note with the specified id.
  400:
    description: A message "Specified note id not found.".
